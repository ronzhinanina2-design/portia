{
  "name": "portia-label-scan-api",
  "private": true,
  "type": "module",
  "description": "Serverless function that powers Portia's AI label scan feature. The rest of Portia is a static site with no build step — this package.json exists only so Vercel builds the /api function. It calls the Claude API directly over fetch (built into Node 18+), with no npm dependencies."
}
